const config = {
  // 网站链接
  url: undefined,
  // 多个网站链接
  urls: [],
  // 提取模式 'singleSite' 单个站点 | 'multipleSites' 多个站点
  extractMode: 'singleSite',
  // 匹配机制 'default' | 'original'
  matchingMechanism: 'default',
  // 图像打包后的文件名
  filename: undefined,
  
}

export { config }
